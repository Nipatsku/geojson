{
  "name": "geojson",
  "version": "1.0.0",
  "description": "node cmd line tool to transform .SHP file to GEOJSON format",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsonfile": "^6.0.1",
    "shapefile": "^0.6.6"
  }
}
